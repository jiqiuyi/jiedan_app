---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 32d1b7e9da980e7fd4c11c9bdd24011a_87b1646aa68c11f199d2525400287e28
    ReservedCode1: 5nFN5pc3z9+gf2lLy1VW8ec2lTFk3CKDKWL6k3e8sfDr+U1lnDqRKyv15t7YeIybw0trI9QRC4S3uzo0hMO+WvX08ZBKQ4lF+4CNtrKof8ZUZcSo97mZ4+biLN9cnNZbqonsV6mANFroW42ZNg2MXFsHCEZkAzKegu33GIK/aAdi9U6jjLbIrCyRNmM=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 32d1b7e9da980e7fd4c11c9bdd24011a_87b1646aa68c11f199d2525400287e28
    ReservedCode2: 5nFN5pc3z9+gf2lLy1VW8ec2lTFk3CKDKWL6k3e8sfDr+U1lnDqRKyv15t7YeIybw0trI9QRC4S3uzo0hMO+WvX08ZBKQ4lF+4CNtrKof8ZUZcSo97mZ4+biLN9cnNZbqonsV6mANFroW42ZNg2MXFsHCEZkAzKegu33GIK/aAdi9U6jjLbIrCyRNmM=
---

# v1.15.0 优化说明

> 接单管家 App 四项体验优化。
> 前端：`D:\dev\jiedan_app`　后端：`D:\dev\jiedan-backend\bin\server.dart`
> 交付日期：2026-09-02

---

## 一、四项改动概览

| # | 改动 | 涉及文件 |
| --- | --- | --- |
| 1 | 从「仅服务器 / 本地+服务器」切回「仅本地」时弹两档确认（保留 / 删除服务器数据） | `storage_mode_page.dart` / `sync_service.dart` / `api_client.dart` + 后端 `server.dart` |
| 2 | 「推广活动」入口移到「升级专业版」正下方 | `settings_page.dart` |
| 3 | 隐私政策全面扩写 | `privacy_policy_page.dart` |
| 4 | 意见反馈改在线实时提交（弃用邮箱） | `feedback_page.dart` / `api_client.dart` / `settings_page.dart` + 后端 `server.dart` |

---

## 二、改动一：切回「仅本地」时删除服务器数据两档确认

### 2.1 交互

- 当用户把存储模式从「仅服务器」或「本地+服务器」切换为「仅本地」时，`storage_mode_page.dart` 弹二次确认对话框，标题提示"是否同时删除服务器上已同步的数据？"，说明删除属不可逆操作、仅影响本账号命名空间、删除前建议先在「数据管理」导出备份。
- 提供两档（**可删可不删，不默认删**）：
  - **a) 仅切回本地，保留服务器数据**：只切本地，服务器数据保留，后续再切回含服务器模式可重新同步回来。
  - **b) 切回本地并删除服务器数据**：切本地同时调用 `DELETE /api/sync/all` 清除本账号在服务器上的全部同步数据。
- 未登录时自动禁用删除档（文案提示"当前未登录，无法删除服务器数据；将仅切换为仅本地模式"）。

### 2.2 后端新接口：`DELETE /api/sync/all`

- 鉴权：`Authorization: Bearer <token>`；无效 / 缺失返回 `401 {ok:false,msg:'未登录'}`。
- 行为：按 token 解析出的 `uid`，从 `syncData` 移除该用户命名空间（含全部表行与墓碑），并持久化 `data.json`。
- 返回：

```json
{ "ok": true, "deletedRows": 2, "uid": 7 }
```

- `deletedRows` 为被清除的同步行数（含墓碑行），供前端提示删除规模。
- 隔离性：仅删除当前用户命名空间，不影响其他用户（已用双账号自测验证）。

### 2.3 前端调用链

`storage_mode_page._onSwitchToLocal` → `SyncService.deleteServerData()` → `ApiClient.deleteServerSync()`（`_call('DELETE','/api/sync/all')`）。
删除成功后 `SyncService` 将本地同步水位 `sync_lwm` 清零（防止后续误以为服务器还有增量），并展示"已删除 N 条服务器数据"提示；删除失败则回滚为仅本地、提示删除失败可稍后重试。

---

## 三、改动二：推广活动入口位置

- `settings_page.dart` 中将「推广活动」ListTile 移至「升级专业版」入口**正下方**（紧跟其后，二者之间以 Divider 分隔）。
- 原「服务与支持」区中的「推广活动」入口已移除，布局归类为：账户与订阅区（升级专业版 → 推广活动）→ 资金区 → 数据与存储区 → 服务与支持区。

---

## 四、改动三：隐私政策扩写

`privacy_policy_page.dart` 由简版扩写为十条详尽条款：

1. **我们收集哪些信息**：账号信息（手机号 / 哈希密码 / 昵称）、设备与运行信息（版本 / 设备型号 / OS 版本，不含位置通讯录相册等）、业务数据（客户 / 项目 / 收款 / 报价 / 待收款 / 里程碑 / 合同）、订阅与消费信息。
2. **业务数据的存储方式（三选一，均免费）**：完整说明「仅本地 / 仅服务器 / 本地+服务器」三态，明确数据存放于用户手机或用户本人账号下的服务器空间。
3. **信息如何使用**：核心功能、账号登录与订阅校验、意见反馈处理、保障安全、授权推送。
4. **是否与第三方共享**：明示不与广告商共享 / 不售租个人信息、无三方广告 SDK；仅账号 / 订阅 / 推广 / 支付相关云端交互及用户主动开启的服务器模式同步。
5. **数据安全措施**：token 鉴权、HTTPS + 证书固定（正式部署后）、密码 bcrypt 加盐哈希、自签名校验与混淆防重打包、云端按 uid 命名空间隔离。
6. **数据保留与删除**：本地数据可随时增删并支持导出备份；服务器数据在切回仅本地时可选择一键删除；账号注销 / 云端数据清除可通过联系我们处理。
7. **您的权利**：导出 / 导入 / 删除 / 查询与更正。
8. **未成年人条款**：面向成年用户，未成年人需监护人同意。
9. **隐私政策的更新与生效**。
10. **联系我们**：通过应用内「意见反馈」在线提交（实时送达），移除原邮箱引导。

生效日期与最近更新同步为 2026-09-02，版本号展示随 `AppConfig.version` 自动为 v1.15.0。

---

## 五、改动四：在线意见反馈（弃用邮箱）

### 5.1 后端新接口：`POST /api/feedback`

- 鉴权：`Authorization: Bearer <token>`；无效返回 `401`。
- 请求体：

```json
{ "type": "bug", "content": "界面卡顿", "contact": "wx:test01" }
```

- `type` 为空默认 `suggestion`（前端传枚举名 `bug / suggestion / other`）；`content` 必填，空内容返回 `400 {ok:false,msg:'反馈内容不能为空'}`；`contact` 选填。
- 落盘：追加到 `data.json` 的 `feedback` 列表，字段 `{id, uid, type, content, contact, createdAt}`，`uid` 为提交人账号、`createdAt` 为服务端毫秒时间戳，开发者实时可见。
- 返回：

```json
{ "ok": true, "feedbackId": 14 }
```

### 5.2 App 端

- `feedback_page.dart` 重写为在线反馈页：类型选择（Bug / 更新建议 / 其他，ChoiceChip）、内容多行输入（500 字上限，按类型给占位提示）、选填联系方式、提交按钮。
- 未登录提交时引导去登录（在线反馈需账号跟踪与回复）。
- 提交成功后：后端返回 `feedbackId`，弹窗提示"反馈（#id）已实时提交到服务器，我们已收到"；同时写入本地 `feedbacks` 表，「我的反馈」列表（AppBar 右上角收件箱图标进入）统一展示历史反馈。
- 提交失败错误以 SnackBar 提示，不丢内容。
- `settings_page.dart`「服务与支持」区「意见反馈」入口保留，subtitle 更新为"在线提交 Bug 或更新建议，实时收到"（不再引导邮箱发送）。

---

## 六、验收清单对照

| 验收项 | 状态 |
| --- | --- |
| 切回仅本地弹两档确认（保留 / 删除，不默认删） | ✅ storage_mode_page + DELETE /api/sync/all |
| 删除接口按 uid 清除命名空间并返回行数 | ✅ `{ok, deletedRows, uid}`，data.json 持久化 |
| 删除隔离性（A 删不影响 B） | ✅ B 拉取仍为 1 条，A 删除后为 0 |
| 未登录删除返回 401 | ✅ |
| 推广活动移到升级专业版正下方 | ✅ settings_page |
| 隐私政策扩写（≥ 九项覆盖） | ✅ 十条条款 |
| 意见反馈在线提交（弃用邮箱） | ✅ POST /api/feedback + 新反馈页 + 我的反馈 |
| 空反馈内容返回 400 | ✅ |
| flutter analyze 零问题 | ✅ No issues found |
| 后端 dart analyze 零问题 | ✅ No issues found |
| flutter build apk --release 成功 | ✅ app-release.apk（53.9MB，v1.15.0+16） |
| 后端本地自测新接口 | ✅ 见第六节：register→push→delete-all→pull 隔离、feedback 往返 |
| README 版本记录更新 | ✅ 追加 v1.15.0 行与设计要点 |
| git 提交并推 master + main | ✅ |

---

## 七、剩余待办（非本次范围）

1. **服务器部署**：`server.dart` 需部署到线上（systemd / docker），并把 `data.json` 落持久卷 + 定期快照；反馈列表建议后续做后台查看面板或邮件订阅提醒。
2. **HTTPS 落地**：证书到位后切 `https://域名` 并填 `pinnedCertSha256`，反馈接口与同步接口一并受证书固定保护。
3. **反馈后台**：目前开发者在服务端直接看 `data.json` 的 feedback 列表；可加一个带鉴权的管理接口 / 面板 + 已读 / 已处理状态。
4. **删除服务器数据二次安全层**：当前删除前仅客户端确认；如需更强保障可在删除接口加一次性确认码（如再次输入密码）防误触。
5. **PC 端定时备份**（承接上期待办）：本地导出加密 + 定时自动备份。
*（内容由AI生成，仅供参考）*
