# v1.16.0 反馈回复功能说明

> 接单管家 App「在线意见反馈」新增「作者回复」闭环。
> 前端：`D:\dev\jiedan_app`　后端：`D:\dev\jiedan-backend\bin\server.dart`
> 交付日期：2026-09-02　版本：v1.16.0+17

---

## 一、功能概览

在 v1.15.0 在线反馈（用户提交 → 服务器落盘）基础上，补齐**作者回复 → 用户可见**的闭环：

| # | 端 | 能力 | 涉及文件 |
| --- | --- | --- | --- |
| 1 | 后端 | `feedback` 记录支持作者回复字段 `reply / repliedAt` | `bin\server.dart` |
| 2 | 后端 | 作者回复接口 `POST /api/feedback/<id>/reply`（管理 token / env ADMIN_TOKEN 鉴权） | `bin\server.dart` |
| 3 | 后端 | 作者查询全部反馈 `GET /api/feedback/all` | `bin\server.dart` |
| 4 | 后端 | 用户侧查询自己的反馈 `GET /api/feedback/mine` 扩展返回回复字段 | `bin\server.dart` |
| 5 | 后端 | 内置极简作者管理页 `GET /admin`（内嵌 HTML，零依赖） | `bin\server.dart` |
| 6 | 前端 | 提交反馈记录 `server_id`，失败降级本地并标注「未同步」 | `feedback_page.dart` / `database.dart` |
| 7 | 前端 | 「我的反馈」合并服务器回复（下拉刷新 + 顶部同步），绿色卡片展示作者回复 | `feedback_page.dart` |
| 8 | 前端 | `feedbacks` 表 v12 迁移（`server_id / reply / replied_at / synced`） | `database.dart` / `constants.dart` |

---

## 二、接口契约

### 2.1 作者 / 管理员身份与鉴权方式

作者身份通过 **服务端配置的管理 token** 校验，有两种配置方式（优先取第一种）：

1. **环境变量 `ADMIN_TOKEN`**：启动后端前设置，例如（Windows PowerShell）
   ```powershell
   $env:ADMIN_TOKEN = "my-secret-author-token"
   dart run bin\server.dart
   ```
   Linux / 容器部署时：`ADMIN_TOKEN=xxx dart run bin/server.dart` 或写入 systemd / docker 环境。
2. **内置默认 token `jiedan-admin-2026`**：未显式设置 `ADMIN_TOKEN` 时兜底使用（见 `server.dart` 顶部 `_defaultAdminToken`）。

> 提示：正式部署务必设置 `ADMIN_TOKEN`，不要沿用内置默认值。

作者接口的 token 传递方式（三者任一均可）：

- `Authorization: Bearer <token>`（推荐，管理页与 curl 均可用）
- query 参数 `?token=<token>`
- 请求体字段 `{ "token": "<token>" }`

### 2.2 作者回复：`POST /api/feedback/<id>/reply`

- 鉴权：作者 token（管理 token 或 `ADMIN_TOKEN`）；缺失 / 错误返回 `401 {"ok":false,"error":"未授权"}`。
- 请求体：`{ "reply": "已收到，正在排查，感谢反馈！" }`
- 约束：`reply` 去除首尾空白后为空返回 `400 {"ok":false,"error":"回复内容不能为空"}`；`id` 不存在返回 `404 {"ok":false,"error":"反馈不存在"}`。
- 行为：将 `reply` 写入对应 `feedback` 记录，并刷新 `repliedAt`（服务端毫秒时间戳），立即持久化到 `data.json` 的 `feedback` 列表；对已回复条目可再次提交作「更新回复」。
- 返回：

```json
{ "ok": true, "feedbackId": 11, "reply": "已收到，正在排查，感谢反馈！", "repliedAt": 1788327645411 }
```

### 2.3 作者查询全部反馈：`GET /api/feedback/all`

- 鉴权：作者 token；缺失 / 错误返回 `401`。
- 返回全部反馈（含 `uid`、联系方式、回复状态），最新在前；全量返回（当前数据量小，后续可加分页）。

```json
{
  "ok": true,
  "count": 2,
  "feedbacks": [
    {
      "id": 14, "uid": 13, "type": "bug", "content": "...",
      "contact": "wx:test01", "createdAt": 1788325209078,
      "reply": null, "repliedAt": null
    }
  ]
}
```

### 2.4 用户查询自己的反馈：`GET /api/feedback/mine`

- 鉴权：用户 token（`Authorization: Bearer <token>`）；缺失 / 无效返回 `401`。
- 仅返回当前用户提交的反馈，最新在前；**扩展返回作者回复字段** `reply / repliedAt`（未回复时为 `null`）。

### 2.5 作者管理页：`GET /admin`

- 无需登录，浏览器打开 `http://<服务器IP>:8090/admin` 即返回内嵌 HTML（无对外静态资源依赖）。
- 使用方式：
  1. 在「作者 Token」输入框填入管理 token（`ADMIN_TOKEN` 或默认 `jiedan-admin-2026`）；
  2. 点「登录并加载反馈」→ 调用 `GET /api/feedback/all` 展示全部反馈（类型 / 用户ID / 时间 / 联系方式 / 作者回复）；
  3. 在任一反馈下填写回复内容，点「回复 / 更新回复」→ 调用 `POST /api/feedback/<id>/reply` 提交，成功后自动刷新列表。
- 已回复条目以绿色块展示；该页为后台面板雏形，后续可迭代（分页 / 状态 / 统计 / 邮件订阅）。

---

## 三、数据模型变更

### 3.1 后端 `data.json` 的 `feedback` 条目

v1.15.0 → v1.16.0 新增两个字段（旧数据无回复则 `reply` 返回 `null`）：

```json
{
  "id": 11,
  "uid": 9,
  "type": "bug",
  "content": "界面卡顿",
  "contact": "qhw_test@x.com",
  "createdAt": 1788324660453,
  "reply": "已收到，正在排查，感谢反馈！",
  "repliedAt": 1788327645411
}
```

### 3.2 前端 `feedbacks` 表（dbVersion 11 → 12）

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | INTEGER PK | 本地自增主键 |
| `type` / `content` / `contact` / `created_at` | 原有 | 反馈内容与时间 |
| `server_id` | INTEGER | 服务器反馈 id，提交成功后写入，用于合并作者回复 |
| `reply` | TEXT | 作者回复内容（服务器拉取合并） |
| `replied_at` | INTEGER | 作者回复时间（毫秒时间戳） |
| `synced` | INTEGER | 1=已同步服务器；0=仅本地草稿（离线提交失败） |

`_migrateToV12` 通过 `ALTER TABLE` 增量加列，老用户升级不丢数据。

---

## 四、前端行为

### 4.1 提交反馈（feedback_page.dart）

- 提交成功后本地记录 `server_id=feedbackId, synced=1`。
- 提交失败（网络 / 未登录异常）：内容仍写入本地 `synced=0`，SnackBar 提示「提交失败，已暂存到本地：... 下拉『我的反馈』可重试同步」，不丢内容。

### 4.2 我的反馈列表（MyFeedbackPage）

- 进入页面自动加载本地反馈箱；已登录则自动调用 `GET /api/feedback/mine` 拉取最新回复并**按 server_id 合并**进本地（更新 `reply / repliedAt`，不覆盖用户内容；服务器有条目本地无记录则按服务器数据新增）。
- **下拉刷新** 或 **AppBar 右上同步按钮** 可手动重新拉取。
- 作者回复以**绿色卡片独立展示**：「作者回复」标签（小信封图标）+ 回复时间 + 回复内容，与用户内容视觉区分。
- 离线：`_syncError` 非空时列表底部展示「当前为本地数据（离线），下拉可重新同步作者回复」；本地 `synced=0` 的草稿标注橙色「未同步」角标。
- 未登录：AppBar 同步按钮点击提示「云同步回复需登录后使用」，列表仅展示本地留存。

---

## 五、验收清单对照

| 验收项 | 状态 |
| --- | --- |
| 未授权访问作者接口返回 401（无 token / 错误 token） | ✅ curl 自测 all=401、reply 用错 token=401 |
| 作者正确 token 可查全部反馈 | ✅ `GET /api/feedback/all` 返回 200 + count |
| 非作者（普通用户 token）不可回复 | ✅ 用户 token 调 reply 返回 401 |
| 作者回复成功且落盘 | ✅ `POST reply` 200，`data.json` 持久化 reply / repliedAt |
| 用户 `mine` 接口能拉到回复 | ✅ 回复前 `reply:null` → 回复后返回 reply + repliedAt |
| 空回复返回 400 | ✅ `reply` 为空 / 纯空白返回 400 "回复内容不能为空" |
| 回复不存在的 id 返回 404 | ✅ |
| `/admin` 管理页可访问 | ✅ `GET /admin` 200 返回内嵌 HTML |
| 后端 dart analyze 零问题 | ✅ No issues found |
| 前端 flutter analyze 零问题 | ✅ No issues found |
| flutter build apk --release 成功 | ✅ app-release.apk（见构建日志） |
| README 版本记录更新 | ✅ 追加 v1.16.0 行与设计要点 |
| 产物文档 | ✅ 本说明 |

---

## 六、剩余待办（非本次范围）

1. **服务器部署**：`server.dart` 部署线上（systemd / docker），`data.json` 落持久卷 + 快照；作者管理页建议仅绑定内网 / 管理网段或加访问层。
2. **HTTPS 落地**：证书到位后切 `https://域名` 并填 `pinnedCertSha256`，作者接口与管理页一并受证书固定保护。
3. **管理页迭代**：分页 / 搜索 / 已处理状态标记 / 邮件或企业微信通知新反馈。
4. **反馈类型扩展与附件**：支持图片 / 截图上传与按类型自动分诊。
*（内容由AI生成，仅供参考）*
