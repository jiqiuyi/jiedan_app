# 接单管家 v1.8.0 改造交付说明

- 版本：v1.8.0（build +9）
- 日期：2026-08-29
- 范围：P0/P1/P2 全量整改 + 收款体系升级

---

## 一、收款体系升级（本次核心）

### 1. 收款码改为用户上传
- 收款设置页（设置 → 收款设置）支持**上传微信收款码 / 支付宝收款码**（image_picker 选图，存本地 SQLite）。
- 未配置收款码时，充值 / 订阅 / 收款入口会**预检引导**去设置，不再出现"点了才发现没码"的断裂。

### 2. 收款入口（看板 + 项目双入口）
- **看板页**：右下角新增「收款」FloatingActionButton → 选择项目 → 选微信 / 支付宝 → 展示对应收款码 → 确认到账。
- **项目详情页**：新增「收款」按钮，同样走"选渠道 → 出示收款码 → 确认到账"。

### 3. 收入金额与项目已收联动
- 用户扫码付款后，在 App 内**手动输入实收金额 + 收款类型**并确认到账。
- 确认后自动写入 `payments` 表，项目的**已收金额**由 `projectPaidTotal()` 实时累加汇总，看板本月收入 / 待收尾款同步刷新。
- ⚠️ **说明**：个人收款码（微信/支付宝个人码）没有开放 API，App **无法自动识别到账金额**，所以当前为"出示收款码 + 手动输入确认"。接入官方商户号 + 支付回调后，才能实现"付款自动识别金额、自动入账"。

### 4. 收款类型扩展（支持多次收款）
- 收款类型由 3 种扩展为 **4 种 + 自定义**：定金 / 尾款 / 全额 / 自定义。
- 自定义类型可输入名称（如"中期款""第二笔尾款"），存入 `payments.type_label`。
- 一个项目可**多次收款**，每次收款独立成记录，已收金额自动累加，历史可查可删（删除有二次确认）。

---

## 二、P0（交付阻塞）整改

| 项 | 状态 |
|---|---|
| 隐私政策独立页 `privacy_policy_page.dart` | ✅ 新增，含数据存储说明、业务数据仅存本机、云端同步范围、联系方式等七节 |
| 登录页默认不勾选同意，未勾选禁止提交 | ✅ 改为默认不勾选 + 校验拦截，可点链接打开政策页 |
| 前后端鉴权统一 | ✅ token 统一走 `Authorization: Bearer` 头 |
| HTTP→HTTPS | ⏳ 待办：需正式域名 + 证书，商户号到位后一起做 |
| 真实支付 4 接入点 + 回调链路 | ⏳ 依赖商户号，pay_service 已预留 |

## 三、P1 整改

| 项 | 状态 |
|---|---|
| 看板 N+1 查询优化 | ✅ 改用 `projectPaidTotals()` 批量查询 |
| 看板"完成数"统计修正 | ✅ 改为全量项目统计，不再只看前 5 条 |
| 删除收款记录加二次确认 | ✅ 已加 AlertDialog 确认 |
| 未配置收款码入口预检 | ✅ wallet / paywall / 收款入口均预检引导 |
| 金额精度 int(分) | ⏳ 保留 double（本地记账场景够用），接真实支付时随回调改造切换 |

## 四、P2 整改

| 项 | 状态 |
|---|---|
| 报价单落库 + 历史 | ✅ 新增 quotes 表，可保存 / 查看历史报价 / 再次复制 |
| 搜索防抖 | ✅ 客户 / 项目搜索加 200ms 防抖 |
| 大列表性能 | ✅ 项目 / 客户列表加 `itemExtent` 固定行高 |

---

## 五、验证状态

- `flutter analyze`：No issues found
- 待办：真机安装 v1.8.0+9 验证收款全流程（需用户手机确认）

## 六、尚未完成（依赖外部条件）

1. 真实支付 / 充值 / 提现接入（等微信 / 支付宝商户号）
2. 支付回调自动识别金额 + 自动入账（等商户号）
3. HTTPS + 正式域名（等上线）
4. 应用商店上架资料（隐私政策外链、备案、软著）
