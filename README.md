# 接单管家（JieDan GuanJia）

> 面向自由职业者 / 接单设计师的接单、报价、收款全流程管理工具。
> 本地 SQLite 存储核心业务数据，账号 / 订阅 / 推广 / 钱包同步云端，开箱即用。

一个从 0 到 1 独立开发的 Flutter Android 应用：需求分析 → UI/UX 设计 → 分层编码 → 真机迭代 → 打包发布。

## 功能特性

| 模块 | 说明 |
|------|------|
| 看板 | 本月收入、进行中项目、待收尾款、客户数、项目总数一屏总览 |
| 客户管理 | 客户信息维护、按名称快速搜索 |
| 项目管理 | 状态流转：接单 → 制作中 → 待收尾款 → 完结；已收/待收/结清金额一目了然；项目搜索 |
| 报价单 | 生成报价单，金额输入智能格式校验（防"145 变 541"类误输入） |
| 收款记录 | 定金/尾款/全额三种类型；项目列表快捷登记收款，无需进详情页 |
| 收入统计 | 历史收入按 **年 / 月 / 周 / 自定义区间** 四种维度筛选 |
| 账号体系 | 手机号 + 密码云端登录；订阅 Pro 解锁额度（月 10 / 年 68 / 永久 98，首月 1 元特惠） |
| 意见反馈 | App 内反馈入口，一键唤起邮箱上报 |

## 技术栈

- **Flutter / Dart 3**（Kotlin 2.x 构建）
- **SQLite（sqflite）** 本地数据库，无后端、无服务器
- **SHA-256** 本地密码哈希（带盐 `jiedan@2026|phone|pwd`）
- **全局中文本地化**（Material 组件日期/时间选择器全面中文化）
- 分层架构：`constants(业务参数) → models → database → state/theme → pages/widgets`

## 项目结构

```
lib/
├── main.dart                # 入口，全局中文本地化配置
├── constants.dart           # 硬编码业务参数（额度/价格/费率/状态等）
├── models.dart              # 数据模型
├── database.dart            # SQLite 数据层（建表/迁移/增删改查）
├── app_state.dart           # 全局状态（登录态、订阅、当前用户）
├── theme.dart               # 主题
├── state/                   # 业务状态
├── widgets/                 # 公共组件（收款弹窗等）
└── pages/                   # 页面
    ├── home_shell.dart          # 主框架（五 Tab 常驻）
    ├── dashboard_page.dart      # 看板
    ├── projects_page.dart       # 项目
    ├── project_detail_page.dart # 项目详情
    ├── customers_page.dart      # 客户
    ├── quote_page.dart          # 报价
    ├── income_history_page.dart # 收入统计（四维筛选）
    ├── login_page.dart          # 登录注册
    ├── paywall_page.dart        # 订阅页
    ├── feedback_page.dart       # 意见反馈
    └── settings_page.dart       # 我的/设置
```

## 快速构建

```bash
# 环境：Flutter SDK + JDK 17 + Android SDK
flutter pub get
flutter build apk --release   # 产物：build/app/outputs/flutter-apk/app-release.apk
```

## 功能分层规划（免费版 vs 专业版 VIP）

> 本文档为商业化前的能力边界规划，**本轮仅文档层规划、不强制改代码逻辑**：除已生效的免费额度（客户 1 / 活跃项目 3）与云端 `isPro` 订阅判定外，其余能力当前均可使用，属「产品规划清单」。后续版本按此清单逐步落地硬性门禁。

### 分层原则

- **免费版定位**：单客户 / 少量项目即可闭环的轻量接单记账工具，覆盖「记录-报价-收款-看数」最小可用链路，隐私优先（仅本地存储可用）。
- **专业版 VIP 定位**：面向多客户 / 多项目 / 跨设备经营的专业接单者，解锁无限规模、云端同步与高级经营分析。
- **判定口径**：以云端账号 `isPro` 为准（v1.13.0 起）并动态过期，登录态与订阅绑定，App 本地不做 VIP 判定源。

### 功能分层清单

| 功能 | 免费版 | 专业版 VIP | 说明 / 现状 |
|------|--------|------------|-------------|
| 客户档案 | 1 个 | 无限 | 已生效门禁（freeCustomerLimit） |
| 项目管理 | 活跃 3 个 | 无限 | 已生效门禁（freeProjectLimit） |
| 报价单（简单 / 详细 / PDF 导出） | ✅ | ✅ | 基础能力不设限 |
| 报价状态流转 / 报价模板 | ✅ | ✅ | 提升报价效率的基础能力 |
| 收款（出示收款码 / 多次收款） | ✅ | ✅ | 基础能力不设限 |
| 待收尾款提醒 / 本地通知 | ✅ | ✅ | 基础能力不设限 |
| 数据存储 · 仅本地 | ✅ | ✅ | 隐私兜底，始终免费 |
| 数据存储 · 仅服务器 / 本地+服务器（云端同步） | ⛔ 规划 VIP | ✅ | 跨设备同步属专业权益，规划硬性门禁 |
| 数据备份导出 / 导入恢复 | 基础导出 ✅ | 完整备份策略 | 规划分层，后端可扩展备份历史 |
| 统计看板（近12个月收入 / 应收欠款） | ✅ | ✅ | 基础统计免费 |
| 统计看板 高级分析（自定义日期区间、按月对比、分类汇总按客户/报价类型/项目阶段） | ⛔ 规划 VIP | ✅ | v1.22.0 已实现，规划为专业版权益 |
| 经营对账汇总 | ⛔ 规划 VIP | ✅ | 多维度经营报表属专业分析 |
| 合同 / 发票管理 | ✅ | ✅ | 基础能力（v1.12.0 起已下线，规划按需恢复为免费） |
| 邀请返现 / 推广 | ✅ | ✅ | 增长能力不设限 |
| 意见反馈（在线） | ✅ | ✅ | 服务能力不设限 |
| 隐私政策 / 合规 | ✅ | ✅ | 全版本保障 |

### 落地节奏建议（后续版本按序执行）

1. **v1.23+**：云端同步（服务器 / 双写）接入 VIP 门禁，免费版仅本地存储。
2. **v1.24+**：统计看板高级分析（自定义区间 / 环比 / 分类汇总）加 VIP 门禁，免费版保留近12个月基础图。
3. **v1.25+**：对账汇总接入 VIP；开放「专业版对比」页，免费版内置升级引导（链接至 paywall 现有订阅流程）。

## 版本记录

| 版本 | 内容 |
|------|------|
| v1.0.0 | MVP：客户 / 项目 / 报价 / 收款 / 看板五模块 |
| v1.2.0 | 历史收入查看，按月筛选收款明细 |
| v1.3.0 | 收入筛选扩展为年 / 月 / 周 / 自定义区间；本地账号登录 + 订阅体系 |
| v1.3.1 | 意见反馈入口 + 邮箱上报；日期选择器全面中文化；正式签名 release 包 |
| v1.6.0 | 新价格体系 + 首月 1 元特惠 + 充值 / 提现钱包 + 推广返现 50% |
| v1.7.0 | 云端对接：账号 / 订阅 / 推广 / 支付走阿里云后端 API，邀请自动核验 |
| v1.7.1 | 订阅档位与文案合规调整（永久价 128 → 98，去掉违规表述） |
| v1.8.0 | 交付前全面整改：上传微信/支付宝收款码、看板/项目双收款入口、多次收款自定义类型、隐私政策页、报价单落库历史、搜索防抖等 |
| v1.9.0 | 报价页双 Tab 重构：简单报价（对象+一口价+备注，一屏完成）与详细报价（工时/物料/税率明细）数据隔离，历史区分类型，支持简单转详细 |
| v1.10.0 | 全功能补齐：金额改"分"整数精度存储、报价关联客户 / 转待收款 / 转正式项目、待收尾款看板列表、收入统计曲线 + 客户贡献排行、经营对账汇总、项目里程碑与催款提醒、合同（发票）管理、本地通知 |
| v1.11.0 | 简单报价页交互升级：报价对象改为轻量胶囊切换（替代重色 SegmentedButton）、关联客户档案改为底部弹层选择器；发票管理正式更名为合同管理（草稿/已签/完成 + CSV 导出）；「我的」页按账户与订阅 / 资金 / 经营分析 / 服务与支持四组整理 |
| v1.12.0 | 简单报价对象一页化：客户名称 / 关联项目 / 关联客户档案同屏展示、二选一互斥取消（关联项目可自动回填客户名）；关联项目选择器由下拉改为底部弹层，与客户档案风格统一；下线合同管理（自建记录无法律效力，移除入口与页面） |
| v1.13.0 | 防破解加固（P0-P4）：下线云端模拟支付与 App 模拟收银台、VIP 判定以云端为准并动态过期、备份导入强制重新登录、token 过期/吊销 + 密码 bcrypt + 接口限流、keystore 密码脱库并移出工程、关闭 allowBackup、R8/ProGuard + Flutter 混淆、自签名防重打包校验、HTTPS 证书固定预留 |
| v1.14.0 | 数据存储方式三选一（均不收费）：仅本地（隐私优先，不访问云端）/ 仅服务器（以云端为权威）/ 本地+服务器（双写双向同步）；后端新增 /api/sync/push | pull | merge 按 uid 命名空间隔离、基于更新时间 _ts 的增量同步与删除墓碑、冲突以服务器最新为准；App 数据层 StorageMode 三态 + 7 表 updated_at / 墓碑追踪、设置页「数据存储方式」入口、存量数据首次切服务器一次性全量上传、仅本地模式完全不上传业务数据 |
| v1.15.0 | 四项体验优化：1) 从「仅服务器 / 本地+服务器」切回「仅本地」时弹两档确认——仅切回本地保留服务器数据 / 切回本地并删除服务器同步数据（后端新增 DELETE /api/sync/all，按 uid 清除命名空间，可删可不删不默认删）；2)「推广活动」入口移至「升级专业版」正下方；3) 隐私政策全面扩写（信息收集 / 存储方式三选一 / 使用 / 第三方共享 / 安全措施 / 保留与删除 / 用户权利 / 未成年人 / 更新生效 / 联系）；4) 意见反馈改在线实时提交（弃用邮箱）：后端新增 POST /api/feedback（token 鉴权，按 uid 记录），App 新增反馈页（类型 / 内容 / 选填联系方式）与「我的反馈」列表 |
| v1.16.0 | 反馈回复闭环：作者可在电脑端回复反馈提交者，用户可在 App 「我的反馈」看到作者回复——后端 feedback 记录新增 reply / repliedAt 字段；新增 POST /api/feedback/<id>/reply（管理 token / env ADMIN_TOKEN 鉴权，空回复 400）、GET /api/feedback/all（作者查全部）、GET /api/feedback/mine 扩展返回回复字段；内置 GET /admin 极简管理页（输入管理 token 查看全部反馈并回复）；App「我的反馈」列表在线合并作者回复（下拉刷新 + 顶部同步按钮），离线展示本地并标注「未同步」，feedbacks 表升级 v12（server_id / reply / replied_at / synced） |
| v1.17.0 | 作者后台数据看板（仅后端 + 管理页，无 App 改动）：新增 GET /api/admin/stats（管理 token 鉴权，未授权 401）聚合经营统计——总用户 / 今日新增注册（按 createdAt 当天，服务器本地时区）/ 付费用户数（is_pro 为真 ∪ 存在 status=paid 订单的 uid 去重）/ 今日收入 / 总收入（口径与支付回调一致，基于 paid 订单 amount 汇总）/ 近 7 日新增趋势 / VIP 用户数 / 邀请注册数 / 反馈总数与待回复数 / 存储模式分布（后端未持久化 StorageMode 字段，available=false 并说明以 server 同步过的用户数为下限参考）；GET /admin 管理页新增「数据看板」区块，输入管理 token 后以大数字卡片展示关键指标 |
| v1.18.0 | 支付闭环与管理员体系：客户点「开通专业版」→ 随机金额（9.00~11.00）拉起微信/支付宝付款 → 秒开通，后端订单对账（通知监听抓金额+时间匹配，未匹配进抽查单）；用户 role 字段 + 管理后台（仅管理员可见数据看板/到账监听状态/订单复核）；客户页新建入口移回右下角 FAB（主题蓝）、注册页手机号 11 位校验、到账核对改用系统权威判定 |
| v1.19.0 | 报价单 PDF 导出：简单 / 详细报价均支持一键导出 PDF（内置中文字体），报价单含客户 / 关联项目 / 编号 / 日期基本信息表、明细表格与金额块（小计 / 税费 / 合计）、备注，支持自定义落款（个人/工作室名称 + 联系方式，本地持久化）；新增 pdf / printing 依赖，版本号升至 1.19.0+26 |
| v1.19.1 | 三项产品体验修复：1) 简单报价二次编辑不再误跳详细 Tab——从历史进入编辑时先关闭弹层再切 Tab，并保留下一帧兜底锁定索引，根治弹层关闭动画与 TabBarView 滚动动画并发导致的 Tab 漂移；2) 金额输入统一容错——新增 lib/utils/money_input.dart 的 MoneyInputFormatter，报价（一口价 / 单价 / 物料费 / 税率）、登记收款、出示收款码实收金额、项目约定金额、阶段金额、钱包充值 / 提现等全部金额输入框接入，只允许合法金额（非负、单个小数点、最多两位小数），自动过滤多小数点 / 负号 / 非法字符，配合分转元展示彻底消除异常金额；3) 删除操作二次确认补全——客户 / 项目 / 报价 / 收款 / 阶段删除均弹确认框并附不可恢复后果提示（报价、阶段删除本轮补后果文案），防止误触；版本号升至 1.19.1+27 |
| v1.20.0 | 三项技术加固：1) 数据备份 / 导出强化——导出优先写入系统「下载」目录且文件名带日期（yyyyMMdd_HHmmss），下载目录不可用（Android 旧版）回退应用文档目录再弹分享；导入恢复前仍做文件完整性校验与双层覆盖确认，导出 / 恢复过程新增“进行中”进度反馈卡（转圈 + 文案），结果以 Toast / 明细弹窗反馈；2) 全局异常捕获——新增 lib/services/error_reporter.dart，安装 FlutterError.onError 与 PlatformDispatcher.onError（返回 true 防止崩溃白屏），配合 runZonedGuarded 兜住异步错误，异常按天滚动写应用日志（保留 3 天），并向用户弹友好提示（不暴露底层技术细节）；3) 云端同步冲突处理完善——新增 SyncConflictPolicy（自动采用较新版本 / 每次询问用户）持久化策略，冲突时以时间戳较新为准或进入待决策列表，storage_mode_page 同步区展示冲突数量与解决弹层（单条 / 批量采用云端、保留本地、暂时不管），同步失败弹友好提示并可在设置中重试，消除静默失败；业务数据仍全部存储于本地 SQLite，不破坏本地化红线；版本号升至 1.20.0+28 |
| v1.20.1 | 合规项完善（面向应用商店上架审核）：1) 隐私政策补强——新增「权限使用说明」章节（通知访问仅用于收款到账提醒、存储与文件访问仅用于备份导出 / 导入、不申请位置 / 相机 / 通讯录等无关权限）；「与第三方共享」章节增补第三方服务说明（微信 / 支付宝支付跳转、本地通知不做第三方推送收集设备信息）；「联系我们」补充反馈邮箱（qhw19980901@gmail.com）并保留在线反馈主渠道；更新日期校准；2) LICENSE 改为个人作品版权声明并明确第三方组件许可归属（引用 Flutter / Dart SDK 及各依赖原始 LICENSE / NOTICE），保留原作者版权；业务数据本地 SQLite 红线不受影响；版本号升至 1.20.1+29 |
| v1.21.0 | 三项中优先级产品体验：1) 客户档案完整化——客户资料新增行业 / 来源 / 所在地 / 最近联系时间字段（数据库 v13 迁移补列，兼容旧库），列表展示行业标签与备注 / 所在地 / 最近联系摘要，客户详情页可查看该客户名下全部分项目（名称 / 约定金额 / 状态）与累计收款金额；2) 报价状态流转——新增草稿 / 已发送 / 客户确认 / 已成交 / 已作废五态（QuoteStatus 枚举 + quotes 表 status 列，v13 迁移默认存量转草稿），历史报价列表展示彩色状态标签、点击标签可弹层快速切换状态，处于成交 / 作废态的报价自动从业务列表隐藏（模板 / 历史可查），合约流转 / 转项目与状态切换并存；3) 报价模板——历史报价支持「另存为模板」，新建报价可从模板一键套用（沿用标题 / 明细 / 单价 / 税率 / 备注，可再修改），模板列表带删除；业务数据仍全部存储于本地 SQLite，不破坏本地化红线；版本号升至 1.21.0+30 |
| v1.22.0 | 三项中优先级待办：1) 统计看板优化——收入 / 应收统计页重构为「统计看板」，新增时间范围筛选（本月 / 上月 / 近12个月 / 自定义起止日期，自定义即按周 / 区间统计）、月度环比对比（本月较上月增减额与百分比）、分类汇总三维度切换（按客户 / 按报价类型 / 按项目阶段），保留近12个月收入柱状图；统计口径（报价总额 / 报价类型汇总）自动排除草稿、作废状态报价及报价模板，DB 层新增 quotesTotalInRange / quotesByTypeInRange / customerContributionInRange / incomeByProjectStatus 四方法；2) UI 细节修复——报价页简单 / 详细两个表单 Tab 改为可滚动容器并增大底部内边距（bottom 24→96），修复小屏手机常驻底部操作区遮挡表单内容的问题，两个 Tab 均开启 keyboardDismissBehavior=onDrag，滚动即收起键盘，缓解输入表单滚动时的焦点错位；3) 功能分层规划——README 新增「功能分层规划（免费版 vs 专业版 VIP）」章节（分层原则 + 17 项功能清单 + 落地节奏），明确免费 / 专业版权益边界，本轮仅文档层规划、不改业务逻辑；业务数据仍全部存储于本地 SQLite，不破坏本地化红线；版本号升至 1.22.0+31 |

## 设计要点（踩坑沉淀）

- **看板与列表 UI 加固**：可滚动容器内禁用 `Spacer`（无界高度导致整页空白）；金额展示用 `FittedBox` 缩放防溢出红条
- **真机为纲**：交付前逐 Tab 真机截图核对，杜绝溢出与空白回归
- **本地账号无服务器**：当前为本地方案，后续可平滑迁移 Firebase / Supabase / 自建后端
- **数据存储三态**：业务数据同步范围为客户 / 项目 / 收款 / 报价 / 待收款 / 里程碑 / 合同 7 表；「仅本地」为默认与隐私兜底，绝不访问云端业务接口；「仅服务器 / 本地+服务器」依赖后端 token 鉴权按用户隔离，切换时提示数据合并与导出备份
- **切回仅本地的两档语义**：删除服务器数据（DELETE /api/sync/all）属不可逆操作，客户端弹两档确认且不默认删除；删除前提示先导出备份，删除仅影响当前账号命名空间
- **在线反馈替代邮箱**：反馈实现在线提交后立即落盘服务器 feedback 列表（本地 feedbacks 表同步留存），开发者在服务端实时可见，不再依赖邮件客户端；联系方式改为选填
- **反馈回复闭环合并策略**：服务器 /api/feedback/mine 为作者回复的权威来源，App 拉取后按 server_id 合并进本地 feedbacks 表（更新 reply/repliedAt，不覆盖用户内容）；提交反馈即时保存 server_id，离线提交标记 synced=0 并在列表标注「未同步」，下拉/按钮可重试同步；作者回复在列表内绿色卡片独立展示
- **上架就绪**：已完成正式签名（release keystore 自持），不依赖 debug 密钥
