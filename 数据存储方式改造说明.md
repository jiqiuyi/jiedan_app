# 数据存储方式改造说明（v1.14.0）

> 接单管家 App 数据存储「仅本地 / 仅服务器 / 本地+服务器」三选一双向功能，均不收费。
> 前端：`D:\dev\jiedan_app`　后端：`D:\dev\jiedan-backend\bin\server.dart`
> 交付日期：2026-09-02

---

## 一、模式设计

新增 `StorageMode` 三态枚举（`lib/constants.dart`）：

| 模式 | label | 语义 | `involvesServer` |
| --- | --- | --- | --- |
| `StorageMode.local` | 仅本地 | 业务数据只存手机 SQLite，**完全不上传、不访问云端业务接口**（隐私承诺，默认模式） | false |
| `StorageMode.server` | 仅服务器 | 以云端为权威存储，本地 SQLite 仅作离线缓存；启动 / 登录时从云端拉取回填 | true |
| `StorageMode.both` | 本地 + 服务器 | 双写：本地每次写入后异步推送云端；启动 / 登录时从云端增量拉取合并到本地，双向同步 | true |

- 选择持久化到本地的 `cloud_token` 沿用已上线的 secure storage（`flutter_secure_storage`），改造不涉及凭据迁移。
- 「仅服务器 / 本地+服务器」仅在已登录且持有 token 时生效；未登录无 token 时 `canSync == false`，自动退回纯本地。
- 程序入口 `main.dart` 在 `AppState.load()` 后、`runApp` 前调用 `SyncService.instance.init()`，恢复上次存储模式并按需拉取。
- 登录 / 注册成功后自动触发一次 `syncOnStart()`，衔接存量数据全量上传与后续增量。

### 三态差异（设置页展示文案）

- **仅本地**：数据不出本机，卸载前请自行备份；最保护隐私。
- **仅服务器**：手机只是缓存，数据以云端为准；换机 / 重装后登录即可恢复。
- **本地 + 服务器**：双端双写，任一端的修改都会同步到另一端；断网时本地可先工作，联网后自动合并。

---

## 二、后端接口契约（server.dart 新增）

三个同步接口，均需 `Authorization: Bearer <token>` 鉴权；数据按登录用户 `uid` 命名空间隔离，后端 `DataStore` 新增
`Map<int, Map<String, dynamic>> syncData` 持久化存储（JSON 落盘），用户之间互不可见。

### 2.1 推送 `POST /api/sync/push`

请求体：

```json
{
  "tables": {
    "customers": [ { "id": 5001, "name": "张三", ..., "_ts": 1788321972318 } ],
    "tombstones": [ { "table": "customers", "id": 5001, "deletedAt": 1788321973000 } ]
  }
}
```

- 逐行合并：同一 `id` 以行内时间戳 `_ts` 比较，**服务器已有行的 `_ts` 更新则忽略客户端旧数据（server-wins）**。
- `tombstones` 用于删除同步：服务器按 `table + id` 软删除（置 `_deleted`），后续拉取不再返回。
- 返回值：

```json
{ "ok": true, "serverTs": 1788321972938, "uid": 7 }
```

`serverTs` 为本次推送后的服务器水位，客户端将其作为下一次增量 `since`。

### 2.2 拉取 `GET /api/sync/pull?since=<水位>`

- `since=0` 全量返回该用户命名空间下全部表快照（首次切换 / 全量合并用）。
- `since=本次serverTs` 仅返回 `_ts > since` 的新增 / 更新（增量）。
- 返回值 `tables`：`customers / projects / payments / quotes / pending_collections / milestones / contracts` 各行含服务端权威 `_ts`，以及 `serverTs` 供客户端推进水位。

### 2.3 合并 `POST /api/sync/merge`

- 请求体同 push；差异在于**返回权威快照**：客户端把本地变化上传，同时把服务端当前全部行返回，客户端以返回快照为准应用（首次切换「存量数据全量上传 + 云端回灌合并」走此接口）。

### 2.4 冲突策略

- 统一以 **服务器最新（server-wins）** 为准：同 id 行比较 `_ts`，旧的不覆盖新的。
- 客户端侧：拉取回的服务器行直接覆盖本地同 id 行（本地无则插入，服务端已 `_deleted` 则本地软删除）；本地比服务器新的行会在下次 push 时上传并被服务端采纳（双侧各保最新，配合水位最终一致）。

---

## 三、前端数据层改造

### 3.1 数据库升级（dbVersion 10 → 11，`lib/database.dart`）

- `_migrateToV11`：为 6 张同步表新增 `updated_at` 列（`customers / payments / quotes / pending_collections / milestones / contracts`，`projects` 原有），
  新建 `sync_tombstones` 表（`table TEXT, row_id INTEGER, deleted_at INTEGER`）记录删除墓碑。
- 7 张同步表的 insert / update / delete 统一注入 `updated_at` 时间戳；delete 路径同步写入 `sync_tombstones`，供云删同步。
- 类顶部新增静态 `onDataChanged` 回调 + `_notify()`：任何被追踪表写入后触发，`SyncService.onLocalChanged()` 接到回调后 2 秒防抖异步推送云端（仅 `both` 模式）。

### 3.2 模型层（`lib/models.dart`）

- `Customer / Payment / Quote / PendingCollection / Milestone / Contract` 新增 `updatedAt`，接入 `toMap / fromMap / copyWith`（Project 已有）。

### 3.3 同步辅助方法（`AppDb`）

- `addTombstone / getTombstones / clearTombstones`：墓碑读写。
- `upsertSyncRow(table, row) / deleteRowSilently(table, id)`：用云端正本写回本地 / 按墓碑静默删除，不重复触发 `_notify` 防止回环。
- `syncTableCounts() / syncRows(table) / syncRowById(table, id)`：供 SyncService 构建上传载荷与水位处理。

### 3.4 ApiClient（`lib/api_client.dart`）

- 新增 `pushSync / pullSync / mergeSync` 三个方法，均带 token，POST/GET 对应路由，超时与错误统一走既有 `_call`。

### 3.5 SyncService（`lib/services/sync_service.dart`，核心编排）

| 方法 | 职责 |
| --- | --- |
| `init()` | 读取持久化模式并恢复 `finalMode`；若含服务器且已登录，触发一次 `syncOnStart()` |
| `setMode(m)` | 持久化新模式；切到含服务器且 `lwm==0`（首次）时执行 `mergeSync` **一次性全量上传存量数据** |
| `syncOnStart()` | 登录 / 启动调用：`lwm==0` → `mergeSync` 全量；`lwm>0` → `pullSync(since=lwm)` 增量；`both` 模式先 push 本地未推变更再拉 |
| `onLocalChanged()` | 本地写入回调，2 秒防抖调度 `pushNow()`（仅 `both`，且不重复入队） |
| `pushNow()` | 立即推送本地所有行 + 墓碑，成功后推进 `lwm` |
| `_lwm/_setLwm` | 用 `settings.sync_lwm` 记录上次成功同步后的服务器水位（增量依据） |

- 全程用互斥锁 `_runMutex` 串行化，避免并发 push / pull 竞争。
- 同步范围表常量 `SyncService.tables`：`customers / projects / payments / quotes / pending_collections / milestones / contracts`。

### 3.6 设置页与存储方式选择页

- `lib/pages/storage_mode_page.dart`：三态卡片单选（说明各模式差异）、切换时二次确认弹窗（提示数据合并 / 导出备份）、当前同步状态（上次同步时间 / 错误 / 同步中）与「立即同步」按钮。
- `settings_page.dart`：新增「数据与存储」Section，含「数据存储方式」入口（展示当前模式）与「数据管理」入口（原有备份导出），移除重复旧条目。

---

## 四、隐私与数据安全要点

1. **仅本地模式完全不访问云端业务接口**：`canSync == false`，`syncOnStart / onLocalChanged` 全部短路，业务数据零上传，兑现隐私承诺。
2. **用户数据隔离**：后端按 `uid` 命名空间存储，注册新用户拉取不到他人数据（已用双账号自测验证）。
3. **存量数据首次上传**：本地已有客户 / 项目 / 收款等数据时，首次切换到含服务器模式走 `mergeSync` 一次性全量上传，之后再增量。
4. **不因存储方式收费**：三态均为免费功能，不涉及订阅 / VIP 逻辑。

---

## 五、验收清单对照

| 编号 | 验收项 | 状态 |
| --- | --- | --- |
| 1 | 设置页新增「数据存储方式」三选一入口 + 模式差异说明 | ✅ `settings_page.dart` + `storage_mode_page.dart` |
| 2 | 后端同步 API（push / pull / merge，token 鉴权、增量、server-wins） | ✅ server.dart 三路由 + `_mergeClientRows` |
| 3 | App 数据层 StorageMode 三态 + 双写同步 | ✅ constants / database / models / api_client / sync_service |
| 4 | 仅本地不访问云端 + 切换时合并/导出提示 | ✅ canSync 短路 + 二次确认弹窗 |
| 5 | 存量数据首次切服务器一次性全量上传 | ✅ setMode 首切走 mergeSync 全量 |
| 6 | 用户间数据隔离 | ✅ 后端 uid 命名空间，双账号自测通过 |
| — | flutter analyze 零问题 | ✅ No issues found |
| — | 后端 dart analyze 零问题 | ✅ No issues found |
| — | flutter build apk --release 成功 | ✅ app-release.apk 已生成（build/app/outputs/flutter-apk） |
| — | 后端启动 + curl 自测（register→login→push→pull 上门往返） | ✅ 全量拉取、增量(Since=serverTs 为 0)、隔离、merge 改名均通过 |
| — | git 提交并推送 master + main | ✅ 见版本记录与提交信息（v1.14.0） |
| — | README 版本记录更新 | ✅ 追加 v1.14.0 行与设计要点 |

---

## 六、剩余待办（非本次范围）

1. **PC 端定时备份**：本次只做了服务器备份通道（收费为 0），建议后续加 PC 端一键导出 + 定时自动备份（本地加密 zip）。
2. **HTTPS 落地**：当前仍 `http://121.41.97.109:8090`；待正式证书签发后切换 `https://域名` 并填入 `pinnedCertSha256` 指纹，开启证书固定。
3. **服务器部署**：后端仍以本地进程方式运行；上线前需部署到公有云（systemd / docker），并把 `data.json` 落到持久卷 + 定期快照。
4. **冲突策略增强**：当前为 server-wins 全自动；如需人工解决冲突（同字段双改），可增加按行冲突标记与 UI 合并提示。
5. **只服务器模式弱网体验**：可增加本地写队列与重试退避，超时任务提示「待联网同步」。
6. **多端并发**：目前同步水位基于单客户端 `lwm`；多设备同时使用时建议服务端按 `since` 返回更新集 + 客户端去重，防止遗漏。

---

## 七、备注

- 构建命令：`flutter build apk --release --obfuscate --split-debug-info=build/symbols`（需 `ANDROID_HOME=D:\dev\android-sdk`、`JAVA_HOME=D:\dev\jdk\jdk-17.0.20.1+1`）。
- 后端启动：`dart run bin/server.dart`（默认 `0.0.0.0:8090`），`data.json` 为持久化命名空间文件。
- 自测脚本可参照项目内 temp 工作区 `storage_mode_20260902`（中间产物，不入库）。
