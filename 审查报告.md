---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 32d1b7e9da980e7fd4c11c9bdd24011a_c3eb2de9a3ad11f1bc17525400826444
    ReservedCode1: 3344r2wm+bMOVzydZ4sWJubP2vBE9YyCsOkT0tLoEXENUloCEN7FUesxylnPV8EYj78Ha7mhkdcgcB+yVlJiSR2jeE8kxxjKWph3ohzlqcMRf2SZDqvpNjV10dGOTzCI2PWQJfuCgYKKtgWKdPa8ZKuEAJoo19zfCJj4pcY/HtS5gqrf908duE+YX2A=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 32d1b7e9da980e7fd4c11c9bdd24011a_c3eb2de9a3ad11f1bc17525400826444
    ReservedCode2: 3344r2wm+bMOVzydZ4sWJubP2vBE9YyCsOkT0tLoEXENUloCEN7FUesxylnPV8EYj78Ha7mhkdcgcB+yVlJiSR2jeE8kxxjKWph3ohzlqcMRf2SZDqvpNjV10dGOTzCI2PWQJfuCgYKKtgWKdPa8ZKuEAJoo19zfCJj4pcY/HtS5gqrf908duE+YX2A=
---

# 接单管家 App 交付前自审报告

- 审查对象：`D:\dev\jiedan_app`（Flutter 项目，lib/ 下 27 个 Dart 源文件，约 5900 行）
- 审查方式：逐文件通读全部源码（main / constants / models / database / app_state / theme / state/ / widgets/ / pages/ / services/）
- 审查日期：2026-08-29
- 审查维度：Bug 排查 / 功能完整性 / 交互与体验 / 性能与卡顿 / 支付交付就绪度

---

## 结论总览

| 维度 | 结论 | 严重度 |
|---|---|---|
| Bug 排查 | 未发现崩溃级致命 Bug，但存在多处逻辑偏差与状态不同步隐患 | 中 |
| 功能完整性 | 核心功能（客户/项目/收款/钱包/看板/备份/反馈）已实现可用；**报价单为半成品（不落库）；支付为模拟实现（未接真实支付）** | 中高 |
| 交互与体验 | 整体流程顺畅，空态/加载态基本齐全；**无独立隐私政策页、登录勾选默认勾选、删除收款记录无二次确认**等需改进 | 中 |
| 性能与卡顿 | 无主线程阻塞级问题；**看板 awaiting 列表存在 N+1 查询、全量 ListView.builder 无分页**，数据量大时有卡顿风险 | 中 |
| 支付交付就绪度 | 通道抽象清晰，但**并非"改三处即可上线"（实际 4 个接入点）**；前后端契约存在字段不一致风险；距"可交付用户"仍缺多项硬性条件 | 高 |

**一句话结论：作为一个 MVP/个人自用工具，工程质量合格；但若目标是"接上微信/支付宝商户号后立即交付用户使用"，当前不达标——支付仅为个人收款码+手动确认的模拟形态，且缺少隐私政策、订单对账、真实支付回调等交付硬条件。**

---

## 一、Bug 排查

### B1.【中】看板"待收尾款"列表 N+1 查询 + 完成数统计偏差 — `lib/pages/dashboard_page.dart`
- 证据：`_refesh()` 中对 `awaiting` 状态项目**逐条**调用 `projectPaidTotal()`（每次一条 SQL），项目多时产生 N+1 查询。
- 证据：`_doneCount` 仅基于 `_recent`（前 5 条最近项目）统计"已完成"数量，而非全量 `_done` 项目数，**看板统计数字与实际不符**（第 5 条之后完成的订单不计入）。
- 建议：改用 `AppDb.projectPaidTotals()` 批量查询（该优化在 database.dart 已实现，看板未复用）；`_doneCount` 从全量项目集合统计。

### B2.【中】报价单"加一行"后可产生空行项目，复制内容含无效行 — `lib/pages/quote_page.dart`
- 证据：`_lines.add(const QuoteLine(itemName: ''))`（约 185 行）新增空行；`_LineCard._emit()` 对空名称/空工时照常输出。
- 证据：`buildQuoteText()` 对 `hours>0` 才写工时、`materialFee>0` 才写物料，但**空 itemName 行仍会输出"1. （空）"**，复制给客户的报价单不专业。
- 建议：生成时过滤空名称行，或加行时禁止空行保存。

### B3.【中】收款码未配置时引导文案与实际不符 — `lib/widgets/show_payment_code.dart`
- 证据：未配置收款码时引导"去设置"（已知，交互合理），但 wallet/paywall 中充值/订阅在未配置收款码时仍允许"出示收款码"，**用户点了才发现没配收款码**，流程断裂。
- 建议：在充值/订阅入口预检收款码是否已配置，未配置先弹引导。

### B4.【低】支付按钮无防重复提交（防抖/幂等） — `lib/pages/wallet_page.dart` / `lib/pages/paywall_page.dart`
- 证据：充值/确认到账/模拟支付按钮点击后未立即禁用（部分有 `_submitting`，部分没有），连点可能重复生成订单/重复入账记录。
- 建议：所有支付/入账/提现类按钮统一加"执行中禁用"态。

### B5.【低】`withValues(alpha:)` 依赖较新 Flutter API
- 证据：全项目多处使用 `withValues(alpha:)`（如 invite_page、projects_page、dashboard 等），该 API 仅存在于 Flutter 3.27+；若目标 SDK 低于此版本将**编译失败**。需确认 pubspec 锁定版本。

### B6.【低】`models.dart` WithdrawAccount.toJson 手动拼接字符串
- 证据：toJson 用字符串拼接（仅替换双引号），若账号/姓名含特殊字符可能产生**解析失败/注入风险**（此前已记录）。导出备份 JSON 时经此序列化。
- 建议：改用 jsonEncode / 结构化字段。

### B7.【低】备份导入导出与"数据只在本地"的产品叙事一致，但**无版本/文件头校验**
- 证据：`backup_service.dart` 导入仅做事务与格式校验（已知完善），但未校验备份文件版本号，未来 dbVersion 升级后旧备份可能导入异常字段。
- 建议：备份文件头写入版本号，导入时做兼容判断。

### B8.【低】状态流转"完结 → 待收尾款"文案——`lib/pages/project_detail_page.dart`
- 证据：`_advanceStatus()` 中 `done → awaiting`（约 60-63 行），`_nextLabel()` 返回"待收尾款（重新开启）"。逻辑本身自洽，但**状态可无限循环流转（done→awaiting→done）**，无"已完成项目不应再流转"的语义约束，误触时数据语义漂移。

---

## 二、功能完整性

对照应有功能清单逐项核对：

| 功能 | 状态 | 证据与说明 |
|---|---|---|
| 看板统计 | ✅ 已实现 | dashboard_page.dart：总额/今日/本月收入、项目计数、近期订单。**但见 B1：完成数统计有偏差** |
| 客户管理 | ✅ 已实现 | customers_page.dart：增删改查、搜索、免费版额度限制、空态引导。完整 |
| 项目管理 | ✅ 已实现 | projects_page.dart：新建、状态流转、搜索、标签筛选、收款登记入口。完整 |
| 报价单 | ⚠️ **半成品** | quote_page.dart：可编辑行、税率、合计，但**只能"复制文本"发送，不落库、无历史、不关联客户/项目、不可导出**。生成即丢失，无法追溯 |
| 收款记录 | ✅ 已实现 | project_detail_page.dart：增删、类型、时间、备注、项目页已收/待收汇总。完整 |
| 收入统计 | ✅ 已实现 | income_history_page.dart（含收入历史页，日期分组） |
| 账号注册登录/订阅 | ✅ 已实现（模拟支付） | login_page.dart 注册/登录；paywall_page.dart createOrder + 模拟收银台 confirmMockPay；app_state 云端权威+本地兜底 |
| 推广返现（邀请码/自动核验/返现/送VIP） | ✅ 已实现（依赖云端） | invite_page.dart：邀请码、好友列表、返现/送VIP 进度；`inviteStats`/`cloudInvitees` 依赖 `_cloudMe`。**注意：`addInvitee` 等在 `_cloudReady` 时直接 return，纯本地无云时推广功能不可用**（设计如此，但需在 UI 上对"未联网"给出提示，当前未提示） |
| 钱包/充值/提现 | ✅ 已实现（模拟） | wallet_page.dart（727 行）：充值走 Channels.recharge 出示码+手动确认；提现走 Channels.withdraw 人工打款；标记到账逻辑完整。**非真实支付** |
| 数据导出导入 | ✅ 已实现 | data_management_page.dart + backup_service.dart：导出 JSON、分享、导入覆盖+确认弹窗、事务包裹 |
| 意见反馈 | ✅ 已实现 | feedback_page.dart：类型选择、本地存档、复制/邮件上报。完整 |
| 中文本地化 | ✅ 已实现 | main.dart 固定 zh_CN locale，全文案中文，无英文残留 |
| 收款码设置 | ✅ 已实现 | payment_code_settings_page.dart：配置微信/支付宝收款码，出示引导 |
| 隐私政策 | ❌ **缺失** | login_page.dart 勾选为弹窗说明，无独立政策页/URL；上架应用商店**硬性缺失** |

**功能层面主要缺口**：
1. **报价单半成品**（不落库、无历史、无分享为图片/PDF，仅复制文本）。
2. **支付/充值/订阅均为模拟**：`paywall` 走云端 createOrder + 本地模拟收银台 confirmMockPay，无真实支付回调；钱包走个人收款码手动确认。**功能"可用"但"不可收款到账"。**
3. **无独立隐私政策页**（影响应用商店上架与合规）。
4. **推广返现依赖云端自动核验**，纯本地（无网/未接云）时无兜底提示，用户会误以为"填了邀请码没生效"。
5. 免费版额度到顶后跳付费墙，但**付费墙本身也接不了真实支付**（死循环：想解锁→付不了款→无法解锁）。

---

## 三、交互与用户体验

### 做得好的地方
- 空态覆盖完整：客户空、项目空、收款记录空、好友列表空、反馈空均有引导文案。
- 加载态统一：各列表页均有 `CircularProgressIndicator`。
- 删除客户/项目均有二次确认弹窗（含后果说明）。
- 数据管理页有"先导出再导入"的明确提示。
- 搜索框带清除语义、键盘 action 为 search。

### 问题清单

### U1.【中】登录页"同意隐私政策"默认勾选 — `lib/pages/login_page.dart`
- 证据：`_agree` 初始为 `true`（此前已核对），默认勾选合规上存在风险（国内应用商店审核红线），且无独立政策页可点开。

### U2.【中】删除收款记录无二次确认 — `lib/pages/project_detail_page.dart`
- 证据：`_deletePayment()` 直接 `deletePayment` 后刷新，无确认弹窗；收款记录是**资金流水**，误删不可恢复。建议与删除项目一致加确认。

### U3.【中】删除客户不校验其下项目 — `lib/pages/customers_page.dart`
- 证据：弹窗说明"其下的项目记录不会删除"，但删除后客户名下项目变成"未知客户"（`_customerName` 兜底），**无重新归属能力**，语义漂移且不可恢复。

### U4.【中】支付流程断裂：未配置收款码时仍展示充值/订阅/收钱入口
- 见 B3。充值/订阅/出示收款码前应预检"收款码是否已配置"，未配置先引导，而不是到出示时才提示。

### U5.【低】充值"手动确认到账"依赖人工，无防误操作
- wallet_page 充值到账手动确认后余额直接入账，**无"到账金额与实付不一致"的校验提示**，用户填错即入错账。

### U6.【低】订阅购买后无真实支付凭据回执
- paywall 模拟收银台确认后即发 VIP，**无订单号/流水展示、无收据**，用户无法向财务/客服举证。

### U7.【低】报价单无"保存/历史"入口
- 见功能缺口：复制即丢，用户无法回顾历史报价。对"接单"场景是刚需。

### U8.【低】部分 SnackBar 文案与动作不同步
- 如反馈"已唤起邮件App"实际可能唤起失败（`launchUrl` 失败有兜底提示，但主文案先说成功再兜底），措辞建议改为"正在唤起邮件应用"。

---

## 四、性能与卡顿

### P1.【中】看板 awaiting 列表 N+1 查询 — `lib/pages/dashboard_page.dart`
- 见 B1：每项目一条 `projectPaidTotal` SQL。20+ 待收项目时明显拖慢首帧。

### P2.【中】列表无分页/懒加载 — projects_page.dart / customers_page.dart / income_history_page.dart
- 证据：全部使用 `ListView.builder` + 全量内存列表（`_projects` / `_customers` 一次性 `getProjects()`/`getCustomers()`），无 `itemExtent`、无分页、无 `ListView.separated` 优化。本地数据规模下可接受，但**长期使用积累上千条收款/项目后首帧与滚动会掉帧**。
- 建议：收款记录可考虑分页或时间范围切片；大列表加 `itemExtent`。

### P3.【低】搜索无防抖 — projects_page / customers_page
- 证据：`onChanged: (v) => setState(() => _query = v)`，每次键入即全量过滤重建。客户/项目数据量大时逐键卡顿。建议 `debounce 200ms`。

### P4.【低】`Ticker.sync` 同步广播刷新 — `lib/state/ticker.dart`
- 证据：sync broadcast，所有订阅页同步重建。数据量小时无感；与 P2 叠加时可能放大卡顿。

### P5.【低】主线程无阻塞
- 未发现 `await` 缺失导致的同步 IO 或大计算；DB 操作均为异步。**结论：无崩溃级卡顿，属数据规模增长型风险。**

---

## 五、支付交付就绪度（重点）

### 5.1 通道抽象结构评价
`lib/services/pay_service.dart`（144 行）结构清晰、值得肯定：
- 抽象：`PayChannel`（createPayment/query）、`WithdrawChannel`（withdraw）、`RechargeChannel`（createRecharge）。
- 注册表：`Channels.pay / Channels.recharge / Channels.withdraw` 静态字段，业务页面只面向 Channels 编程，**通道替换不需要改页面**——这是正确的面向接口设计。
- MVP 实现：`QrCodePayChannel`（个人收款码+手动确认）、`ManualWithdrawChannel`（人工打款）、`QrCodeRechargeChannel`（个人收款码+手动确认）。

### 5.2 "改三处即可上线"的说法不成立（实际 4+ 处 + 契约对齐）
文件头注释称"只需做三件事"，但实际接入点有 **4 个**：
1. `QrCodePayChannel.createPayment`（收款接入点①，注释自述）
2. `QrCodePayChannel.query`（查单接入点②）
3. `ManualWithdrawChannel.withdraw`（提现接入点③）
4. `QrCodeRechargeChannel.createRecharge`（充值接入点④）

且除此之外还需要：
- **回调/轮询**：`query()` 接口无任何调用方（全项目搜索无引用），真实支付的回调通知（微信回调 URL / 轮询查单）**完全没有落地路径**——接了商户号，付完款订单状态也无法自动更新，仍需手动确认。
- **签名与安全**：支付/转账需服务端签名（微信 v3 需证书、平台证书、回调验签），`pay_service.dart` 未预留任何签名/验签结构，需要后端配合。

### 5.3 前后端契约核对（121.41.97.109:8090）
来自 `constants.dart` / `api_client.dart` / `app_state.dart` 的核对结果：
- `apiBaseUrl = http://121.41.97.109:8090`（**http 明文**，非 https——正式交付支付场景为安全红线）。
- 鉴权传递**不一致**：`me()` 将 token 放 URL query；`createOrder` / `mockPay` 将 token 放 body。建议统一为 `Authorization` 头。
- 错误判定：`_call` 以 `ok==false` 或 `401` 判错；`401` 时是否触发重新登录/踢出未确认（此前核对未发现统一 401 处理）。
- 订阅走 `createOrder` + `confirmMockPay`（模拟收银台）：**该接口名含 mock，明显是联调桩**，真实支付需后端替换为正式下单/支付/回调三接口，前端契约需同步。
- 钱包充值/提现目前**完全本地**（Channels 手工模式），未与 8090 服务同步余额与流水——若走云端钱包，前后端需新增充值回调、余额、提现审批等接口。

### 5.4 真正交付用户前还缺什么（硬性清单）
1. **真实支付渠道**：微信/支付宝官方商户号 + APP 支付/收款 API，替换 4 个模拟实现；或接入合规服务商（虎皮椒/PAYJS 等）托管收款。
2. **支付回调/对账**：回调地址 + 验签 + 本地订单号 ↔ 平台流水号映射 + 日终对账；当前 `query()` 无调用方，回调链路为零。
3. **服务端钱包改造**：充值/提现流水若要走云端（订阅已走云端），需新增：充值回调入账、提现申请→审批→打款状态机、余额查询接口；当前提现为纯本地"人工打款"，无后端记录。
4. **HTTPS**：`http://121.41.97.109:8090` 必须换 https + 正式域名（明文 HTTP 传 token/订单是支付类 App 的致命合规问题）。
5. **隐私政策**：独立政策页 + 登录默认不勾选 + 可点击查看全文（应用商店硬性审核项）。
6. **商户资质与经营资质**：营业执照、支付商户号、软件著作权/ICP 备案、App 备案（国内安卓市场要求）。
7. **订单对账与客服**：用户侧订单/收据展示（当前模拟支付无凭据）；退款、客服入口缺失。
8. **金额精度与一致性**：`double` 全程用于金额计算（models/quote/wallet），正式收款场景应改用 `int(分)` 或 `Decimal`，避免浮点误差在分账/对账时出错。

---

## 六、整改优先级建议（P0 交付阻塞 / P1 应改 / P2 可选）

| 优先级 | 事项 | 对应条目 |
|---|---|---|
| P0 | 隐私政策独立页 + 登录默认不勾选 | U1 / 功能缺口 |
| P0 | 真实支付接入（4 接入点 + 回调链路 + 订单凭据） | 5.2 / 5.4 |
| P0 | HTTP → HTTPS | 5.4 |
| P0 | 前后端契约对齐（token 统一、createOrder/mockPay 换真实接口） | 5.3 |
| P1 | 看板 N+1 查询优化 + 完成数统计修正 | B1 / P1 |
| P1 | 删除收款记录加二次确认 | U2 |
| P1 | 未配置收款码时入口预检引导 | U3 / B3 |
| P1 | 金额计算改 int(分) | 5.4-8 |
| P2 | 报价单落库 + 历史 | 功能缺口 |
| P2 | 搜索防抖、大列表分页 | P2 / P3 |
| P2 | WithdrawAccount 序列化改为结构化 JSON | B6 |

---

*本报告基于对 lib/ 下全部源码的逐文件通读，所有问题均标注了文件路径与证据；涉及"此前已核对"的内容（api_client/app_state/database/backup/dashboard/payment_dialog/show_payment_code/wallet/paywall/login 等）来自同一轮全量阅读，证据一致性已交叉确认。*
*（内容由AI生成，仅供参考）*
